# pip 호환용 — 권장 설치 방법은 pyproject.toml + uv 입니다.
# uv sync            → 기본 설치
# uv sync --extra all → 전체 설치
#
# pip 사용 시: pip install -r requirements.txt
# Python 3.10+ 필수 / Python 3.11+ 권장

# ── 필수 ──────────────────────────────────────────────────────────────────────
tornado>=6.3,<7
pyotp>=2.9

# ── 권장 (없으면 /proc 폴백 또는 QR 생략) ────────────────────────────────────
psutil>=5.9
qrcode>=7.4

# ── 선택: 웹 터미널 + 에이전트 CLI ──────────────────────────────────────────
terminado>=0.17                        # 0.17.1 이상 검증됨
websockets>=12                         # cerberus_ctl exec / 봇 WS 클라이언트

# ── 선택: 봇 ─────────────────────────────────────────────────────────────────
python-telegram-bot[all]>=21,<23       # v22 API 변경 대응 전까지 상한 유지
slack-bolt>=1.18
slack-sdk>=3.0

# ── 개발/테스트 ───────────────────────────────────────────────────────────────
pytest>=7.0
